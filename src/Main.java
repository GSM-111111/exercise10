public class Main {
    public static void main(String[] args) {
        // 创建 StoreOneA 对象，参数如文件指定
        StoreOneA store = new StoreOneA(true, 100000, 12, true);
        store.showStoreInfo(); // 调用方法显示信息
    }
}

